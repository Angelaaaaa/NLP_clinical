1. should i record the distance of word as well in pandas(for calculating average distance) - average distance (done)
2. when dealing with ignored words, should i skip them or they are not treated as a unit of distance and inluded in window size


json attritubes name change to something.something (do it later)
add default, individual ,enforce config files (later)

ignore symbols like comma
make it case insensitive (done)



target keywords(stemming), document context, extended terminology for checking values(not manditory), specify target values(for data config)