each row need document id
do not need to pass in config again
add in the statistical analysis in init
stas_window = 20 <- make it variable
make it importable

DOC_ID IN PARSER
action: flush, clear


one single unit
fix find and its window, find the upper level not the same level

statistics 
for every unique token per document, include parameter(window size, dault(5)), pass patameter in init (stat_window).